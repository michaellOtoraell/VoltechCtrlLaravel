{
    "buildCommand": "composer install --no-dev && php artisan storage:link && chmod -R 777 storage bootstrap/cache",
    "startCommand": "php artisan config:clear && php artisan view:clear && php artisan serve --host=0.0.0.0 --port=${PORT}",
    "healthcheckPath": "/"
}
